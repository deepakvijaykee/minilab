# Sample Outputs

The script prints a held-out GSM8K subset accuracy after training:

```text
GSM8K test subset (...) accuracy: ...
```

Short runs validate the pipeline; longer runs are needed for meaningful math
improvement.
