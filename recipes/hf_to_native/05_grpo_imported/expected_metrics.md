# Expected Metrics

This is a tiny RLVR sanity run on imported weights.

- Rewards may be sparse and noisy at the default scale.
- `run_metrics.json` should be written under `checkpoints/imported/<model>-grpo/`.
- On CUDA, peak allocator memory is recorded.
